

View your app in AI Studio: https://ai.studio/apps/drive/1-9bjh_uwp67pYFqPAWMrVONo9Ox7zt-E

## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key
3. Run the app:
   `npm run dev`
   

# commit your backend to GitHub
git init

git add .

git commit -m "Deploy backend"

git push origin main
